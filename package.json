{
  "name": "claude-do",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace @claude-do/site",
    "build": "npm run build --workspaces",
    "build:site": "npm run build --workspace @claude-do/site",
    "preview": "npm run preview --workspace @claude-do/site",
    "cf:dev": "npm run cf:dev --workspace @claude-do/site",
    "cf:deploy": "npm run cf:deploy --workspace @claude-do/site"
  }
}
